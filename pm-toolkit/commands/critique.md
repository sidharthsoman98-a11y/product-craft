---
description: Attack a deliverable the way a hostile interviewer would, score it, name one fix
---
Use the `red-team` skill on: $ARGUMENTS

Run the failure catalogue by name, quote the weakest sentence verbatim, score every rubric
dimension, give the verdict, and name exactly one highest-leverage fix.
